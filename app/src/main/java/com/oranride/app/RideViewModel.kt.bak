package com.oranride.app

import androidx.compose.runtime.mutableStateOf
import androidx.lifecycle.ViewModel

class RideViewModel: ViewModel() {
    var accepted = mutableStateOf(false)

    fun acceptRide(){
        accepted.value=true
    }

    fun rejectRide(){
        accepted.value=false
    }
}